# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env.example
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local dev artifacts
/screenshots/
/.playwright-mcp/
/logs/

# X automation agent data
/data/backups/
/data/weekly-plan.json

# Supabase sync state (local only)
/data/_sync-state.json

# X reply v2 (個人の対象URL・生成ログはコミットしない)
data/x/reply-targets.txt
data/x/replies.jsonl
data/x/posts.jsonl
data/x/ideas.jsonl
.env.local
.env.production

# 手動記入するワークシート。リモート側の再生成とローカルの記入内容が
# 衝突するためGit管理から外す（再生成は --init でいつでも可能）
scratch/manual-affiliate-links.txt
scratch/
data/price-history.json
