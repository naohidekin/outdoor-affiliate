# Hero Image System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全53記事のページトップに3スタイル自動切替のヒーロー画像を追加し、記事内製品写真のimageUrl欠損を補完する。

**Architecture:** slugパターンで記事タイプを判定（tile/split/photo）し、`HeroImage`統合コンポーネントが適切なスタイルを選択する。Photoタイプはarticles.jsonの`eyecatch`フィールドに永続保存したUnsplash URLを使用。

**Tech Stack:** Next.js 15 App Router, Tailwind CSS, Next/Image, Unsplash API (UNSPLASH_ACCESS_KEY in .env.local)

---

## ファイル一覧

| ファイル | 種別 | 役割 |
|---------|------|------|
| `src/lib/types.ts` | 修正 | Article型に`eyecatch?: string`追加 |
| `next.config.ts` | 修正 | images.unsplash.com / imageflux.jp ドメイン追加 |
| `src/lib/hero-type.ts` | 新規 | getHeroType(slug) 判定ロジック |
| `src/components/HeroPhoto.tsx` | 新規 | 単体写真ヒーロー |
| `src/components/HeroTile.tsx` | 新規 | 製品画像タイルグリッド |
| `src/components/HeroSplit.tsx` | 新規 | 対角線スプリット |
| `src/components/HeroImage.tsx` | 新規 | 統合コンポーネント（3スタイル自動切替） |
| `src/app/articles/[slug]/page.tsx` | 修正 | ブレッドクラム直後にHeroImage挿入 |
| `scripts/fetch-unsplash-eyecatch.mjs` | 新規 | guide記事のeyecatch一括取得スクリプト |
| `data/articles.json` | 修正 | 18件のguide記事にeyecatch URL追加 |

---

## Task 0: Supabase `articles` テーブルに eyecatch カラム追加 + db.ts 更新

**Files:**
- Modify: `src/lib/db.ts` (toArticle / fromArticle)

> ⚠️ このタスクは Task 1 より先に実行すること。本番はSupabaseから記事を読み込むため、カラムなしでは eyecatch が取得されない。

- [ ] **Step 1: Supabase に eyecatch カラムを追加**

```bash
curl -X POST \
  "https://pjyoqvwiixzpxbvchvrf.supabase.co/rest/v1/rpc/sql" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqeW9xdndpaXh6cHhidmNodnJmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjE1NzA4MCwiZXhwIjoyMDkxNzMzMDgwfQ.TkB_IVb0i8tX_rZPSwq7X9uZ1MKTE6ahxQrVmCqkM0Q" \
  -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqeW9xdndpaXh6cHhidmNodnJmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjE1NzA4MCwiZXhwIjoyMDkxNzMzMDgwfQ.TkB_IVb0i8tX_rZPSwq7X9uZ1MKTE6ahxQrVmCqkM0Q" \
  -H "Content-Type: application/json" \
  -d '{"query": "ALTER TABLE articles ADD COLUMN IF NOT EXISTS eyecatch TEXT;"}'
```

Expected: HTTP 200 または 204

もし上記が失敗する場合は Supabase ダッシュボードの SQL Editor で以下を実行:
```sql
ALTER TABLE articles ADD COLUMN IF NOT EXISTS eyecatch TEXT;
```

- [ ] **Step 2: db.ts の toArticle に eyecatch を追加**

`src/lib/db.ts` の `toArticle` 関数内、`generationMeta` 行の後に追加:

変更前（末尾）:
```ts
    generationMeta: row.generation_meta as Article["generationMeta"],
  };
}
```

変更後:
```ts
    generationMeta: row.generation_meta as Article["generationMeta"],
    eyecatch: (row.eyecatch as string) || undefined,
  };
}
```

- [ ] **Step 3: db.ts の fromArticle に eyecatch を追加**

`fromArticle` 関数内、`generation_meta` 行の後に追加:

変更前（末尾）:
```ts
    generation_meta: article.generationMeta ?? null,
  };
}
```

変更後:
```ts
    generation_meta: article.generationMeta ?? null,
    eyecatch: article.eyecatch ?? null,
  };
}
```

- [ ] **Step 4: ビルドエラーチェック**

```bash
npx tsc --noEmit 2>&1 | head -10
```

Expected: エラーなし

- [ ] **Step 5: コミット**

```bash
git add src/lib/db.ts
git commit -m "feat: db.ts に eyecatch フィールドマッピング追加"
```

---

## Task 1: 型定義とnext.configにドメイン追加

**Files:**
- Modify: `src/lib/types.ts` (Article interfaceのqualityScore行の後)
- Modify: `next.config.ts`

- [ ] **Step 1: Article型にeyecatchフィールドを追加**

`src/lib/types.ts` のArticle interfaceの末尾（`};`の直前）に追加:

```ts
  // --- ヒーロー画像 ---
  eyecatch?: string;
```

修正後のArticle interface末尾（`qualityScore`行以降）:
```ts
  autoGenerated?: boolean;
  qualityScore?: number;
  scheduledPublishDate?: string;
  generationMeta?: {
    themeId: string;
    model: string;
    retryCount: number;
    generatedAt: string;
  };
  // --- ヒーロー画像 ---
  eyecatch?: string;
}
```

- [ ] **Step 2: next.config.tsにUnsplashとimagefluxドメインを追加**

`next.config.ts` の `images.remotePatterns` 配列末尾に追加:

```ts
      { protocol: "https", hostname: "images.unsplash.com" },
      { protocol: "https", hostname: "plus.unsplash.com" },
      { protocol: "https", hostname: "**.imageflux.jp" },
```

- [ ] **Step 3: TypeScriptビルドエラーがないか確認**

```bash
npx tsc --noEmit 2>&1 | head -20
```

Expected: エラーなし（または既存エラーのみ）

- [ ] **Step 4: コミット**

```bash
git add src/lib/types.ts next.config.ts
git commit -m "feat: Article型にeyecatch追加・Unsplashドメイン許可"
```

---

## Task 2: hero-type.ts（判定ロジック）

**Files:**
- Create: `src/lib/hero-type.ts`

- [ ] **Step 1: hero-type.tsを作成**

```ts
// src/lib/hero-type.ts

export type HeroType = "tile" | "split" | "photo";

/**
 * slugから記事タイプを判定する
 * tile: ランキング・おすすめ系（30件）
 * split: A vs B 比較系（5件）
 * photo: ガイド・チェックリスト系（18件）
 */
export function getHeroType(slug: string): HeroType {
  if (/ranking|%E3%81%8A%E3%81%99%E3%81%99%E3%82%81|budget/.test(slug)) return "tile";
  if (/-vs-|-showdown-|-comparison-|-alternatives/.test(slug)) return "split";
  return "photo";
}
```

※ 日本語slugは実際にはASCIIなのでシンプルに:

```ts
// src/lib/hero-type.ts

export type HeroType = "tile" | "split" | "photo";

export function getHeroType(slug: string): HeroType {
  if (/ranking|budget/.test(slug)) return "tile";
  if (/-vs-|-showdown-|-comparison-|-alternatives/.test(slug)) return "split";
  return "photo";
}
```

- [ ] **Step 2: 判定結果を手動確認**

```bash
node -e "
function getHeroType(slug) {
  if (/ranking|budget/.test(slug)) return 'tile';
  if (/-vs-|-showdown-|-comparison-|-alternatives/.test(slug)) return 'split';
  return 'photo';
}
const articles = JSON.parse(require('fs').readFileSync('data/articles.json','utf-8'));
const counts = {tile:0,split:0,photo:0};
articles.forEach(a => counts[getHeroType(a.slug)]++);
console.log(counts);
"
```

Expected: `{ tile: 30, split: 5, photo: 18 }`

- [ ] **Step 3: コミット**

```bash
git add src/lib/hero-type.ts
git commit -m "feat: getHeroType slug判定ロジック"
```

---

## Task 3: HeroPhoto コンポーネント

**Files:**
- Create: `src/components/HeroPhoto.tsx`

- [ ] **Step 1: HeroPhoto.tsxを作成**

```tsx
// src/components/HeroPhoto.tsx
import Image from "next/image";

interface Props {
  src: string;
  alt: string;
}

export default function HeroPhoto({ src, alt }: Props) {
  return (
    <div className="relative w-full h-[200px] md:h-[300px] overflow-hidden rounded-xl mb-8 bg-mist">
      <Image
        src={src}
        alt={alt}
        fill
        sizes="(max-width: 896px) 100vw, 896px"
        className="object-cover"
        priority
      />
    </div>
  );
}
```

- [ ] **Step 2: コミット**

```bash
git add src/components/HeroPhoto.tsx
git commit -m "feat: HeroPhotoコンポーネント追加"
```

---

## Task 4: HeroTile コンポーネント

**Files:**
- Create: `src/components/HeroTile.tsx`

- [ ] **Step 1: HeroTile.tsxを作成**

```tsx
// src/components/HeroTile.tsx
import Image from "next/image";
import { Product } from "@/lib/types";

interface Props {
  products: Product[];
}

export default function HeroTile({ products }: Props) {
  const images = products.filter((p) => p.imageUrl).slice(0, 6);
  const cols = images.length <= 3 ? images.length : 3;

  return (
    <div
      className="w-full h-[200px] md:h-[300px] overflow-hidden rounded-xl mb-8 bg-mist"
      style={{ display: "grid", gridTemplateColumns: `repeat(${cols}, 1fr)` }}
    >
      {images.map((p) => (
        <div key={p.id} className="relative overflow-hidden bg-white border-r border-line-soft last:border-r-0">
          <Image
            src={p.imageUrl}
            alt={p.name}
            fill
            sizes={`(max-width: 896px) ${Math.floor(100 / cols)}vw, ${Math.floor(896 / cols)}px`}
            className="object-contain p-3"
            priority
          />
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: コミット**

```bash
git add src/components/HeroTile.tsx
git commit -m "feat: HeroTileコンポーネント追加（ランキング用グリッド）"
```

---

## Task 5: HeroSplit コンポーネント

**Files:**
- Create: `src/components/HeroSplit.tsx`

- [ ] **Step 1: HeroSplit.tsxを作成**

```tsx
// src/components/HeroSplit.tsx
import Image from "next/image";
import { Product } from "@/lib/types";

interface Props {
  productA: Product;
  productB: Product;
}

export default function HeroSplit({ productA, productB }: Props) {
  const shortName = (name: string) =>
    name.replace(/\s+\S+$/, "").substring(0, 20);

  return (
    <div className="relative w-full h-[200px] md:h-[300px] overflow-hidden rounded-xl mb-8 bg-mist">
      {/* 左パネル */}
      <div
        className="absolute inset-0 overflow-hidden bg-white"
        style={{ clipPath: "polygon(0 0, 60% 0, 40% 100%, 0 100%)" }}
      >
        <Image
          src={productA.imageUrl}
          alt={productA.name}
          fill
          sizes="60vw"
          className="object-contain p-4"
          priority
        />
        <div className="absolute bottom-3 left-3 bg-black/50 text-white text-xs font-semibold px-2 py-1 rounded backdrop-blur-sm max-w-[40%] truncate">
          {shortName(productA.name)}
        </div>
      </div>

      {/* 右パネル */}
      <div
        className="absolute inset-0 overflow-hidden bg-mist"
        style={{ clipPath: "polygon(60% 0, 100% 0, 100% 100%, 40% 100%)" }}
      >
        <Image
          src={productB.imageUrl}
          alt={productB.name}
          fill
          sizes="60vw"
          className="object-contain p-4"
          priority
        />
        <div className="absolute bottom-3 right-3 bg-black/50 text-white text-xs font-semibold px-2 py-1 rounded backdrop-blur-sm max-w-[40%] truncate text-right">
          {shortName(productB.name)}
        </div>
      </div>

      {/* 中央ライン装飾 */}
      <div
        className="absolute inset-0 pointer-events-none"
        style={{
          background:
            "linear-gradient(to bottom-right, transparent calc(50% - 1px), var(--color-line) calc(50% - 1px), var(--color-line) calc(50% + 1px), transparent calc(50% + 1px))",
        }}
      />
    </div>
  );
}
```

- [ ] **Step 2: コミット**

```bash
git add src/components/HeroSplit.tsx
git commit -m "feat: HeroSplitコンポーネント追加（比較記事用対角線分割）"
```

---

## Task 6: HeroImage 統合コンポーネント

**Files:**
- Create: `src/components/HeroImage.tsx`

- [ ] **Step 1: HeroImage.tsxを作成**

```tsx
// src/components/HeroImage.tsx
import { Article, Product } from "@/lib/types";
import { getHeroType } from "@/lib/hero-type";
import HeroTile from "./HeroTile";
import HeroSplit from "./HeroSplit";
import HeroPhoto from "./HeroPhoto";

interface Props {
  article: Article;
  products: Product[];
}

export default function HeroImage({ article, products }: Props) {
  const type = getHeroType(article.slug);
  const withImages = products.filter((p) => p.imageUrl);

  if (type === "tile" && withImages.length >= 2) {
    return <HeroTile products={withImages} />;
  }

  if (type === "split" && withImages.length >= 2) {
    return <HeroSplit productA={withImages[0]} productB={withImages[1]} />;
  }

  // photo / フォールバック: eyecatch → 最初の製品画像
  const src = article.eyecatch ?? withImages[0]?.imageUrl;
  if (src) {
    return <HeroPhoto src={src} alt={article.title} />;
  }

  return null;
}
```

- [ ] **Step 2: コミット**

```bash
git add src/components/HeroImage.tsx
git commit -m "feat: HeroImage統合コンポーネント（tile/split/photo自動切替）"
```

---

## Task 7: 記事ページにHeroImageを挿入

**Files:**
- Modify: `src/app/articles/[slug]/page.tsx`

- [ ] **Step 1: importを追加**

`src/app/articles/[slug]/page.tsx` のimport群の末尾に追加:

```tsx
import HeroImage from "@/components/HeroImage";
```

- [ ] **Step 2: ブレッドクラムの直後にHeroImageを挿入**

ブレッドクラム`</nav>`の直後（現在の`<h1>`の直前）に以下を追加:

変更前:
```tsx
          </nav>

          <h1 className="text-3xl md:text-4xl font-semibold tracking-tight text-ink-strong leading-tight mb-5">
```

変更後:
```tsx
          </nav>

          <HeroImage article={article} products={products} />

          <h1 className="text-3xl md:text-4xl font-semibold tracking-tight text-ink-strong leading-tight mb-5">
```

- [ ] **Step 3: devサーバーで確認**

```bash
npm run dev
```

ブラウザで以下の3種類を確認:
- タイル: http://localhost:3000/articles/gas-lantern-ranking
- スプリット: http://localhost:3000/articles/landlock-vs-landnest-shelter
- フォト(eyecatch未設定のフォールバック): http://localhost:3000/articles/family-camp-first-time-guide

- [ ] **Step 4: スマホ幅(390px)で表示確認**

Chrome DevToolsでiPhone SE(390px)に切り替えて確認。
高さ200px・画像はみ出しなし・ラウンドコーナーが見えること。

- [ ] **Step 5: コミット**

```bash
git add src/app/articles/[slug]/page.tsx
git commit -m "feat: 記事ページにHeroImage追加（ブレッドクラム直後）"
```

---

## Task 8: Unsplash eyecatch一括取得スクリプト

**Files:**
- Create: `scripts/fetch-unsplash-eyecatch.mjs`
- Modify: `data/articles.json`（18件のguide記事にeyecatch追加）

- [ ] **Step 1: スクリプトを作成**

```js
// scripts/fetch-unsplash-eyecatch.mjs
import { readFileSync, writeFileSync } from "fs";
import { join, dirname } from "path";
import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = join(__dirname, "..");

// .env.local を読み込む
const envPath = join(ROOT, ".env.local");
const envContent = readFileSync(envPath, "utf-8");
for (const line of envContent.split("\n")) {
  const eq = line.indexOf("=");
  if (eq > 0 && !line.startsWith("#")) {
    const key = line.slice(0, eq).trim();
    const val = line.slice(eq + 1).trim();
    if (!process.env[key]) process.env[key] = val;
  }
}

const ACCESS_KEY = process.env.UNSPLASH_ACCESS_KEY;
if (!ACCESS_KEY) throw new Error("UNSPLASH_ACCESS_KEY が未設定");

const args = process.argv.slice(2);
const dryRun = args.includes("--dry-run");
const force = args.includes("--force");

function getHeroType(slug) {
  if (/ranking|budget/.test(slug)) return "tile";
  if (/-vs-|-showdown-|-comparison-|-alternatives/.test(slug)) return "split";
  return "photo";
}

function slugToQuery(slug) {
  // スラッグからクエリ生成: 年サフィックス除去・ハイフン→スペース・不要語除去
  const words = slug
    .replace(/-\d{4}(-\d+)?$/, "")
    .replace(/-/g, " ")
    .replace(/\b(ranking|guide|checklist|tips|beginner|review|camp|camping|gear|outdoor)\b/g, "")
    .replace(/\s+/g, " ")
    .trim();
  return `camping outdoor ${words || slug.replace(/-/g, " ")}`;
}

async function fetchUnsplash(query) {
  const url =
    `https://api.unsplash.com/search/photos` +
    `?query=${encodeURIComponent(query)}&orientation=landscape&per_page=1`;
  const resp = await fetch(url, {
    headers: { Authorization: `Client-ID ${ACCESS_KEY}` },
  });
  if (resp.status === 403) throw new Error("Rate limit or invalid key");
  if (!resp.ok) throw new Error(`Unsplash API ${resp.status}`);
  const data = await resp.json();
  return data.results?.[0]?.urls?.regular ?? null;
}

const articles = JSON.parse(readFileSync(join(ROOT, "data/articles.json"), "utf-8"));
let count = 0;

for (const a of articles) {
  if (getHeroType(a.slug) !== "photo") continue;
  if (a.eyecatch && !force) {
    process.stdout.write(`SKIP (already set): ${a.slug}\n`);
    continue;
  }

  const query = slugToQuery(a.slug);
  process.stdout.write(`Fetching: ${a.slug}\n  query: "${query}"\n`);

  try {
    const imageUrl = await fetchUnsplash(query);
    if (imageUrl) {
      process.stdout.write(`  → ${imageUrl.substring(0, 80)}...\n`);
      if (!dryRun) a.eyecatch = imageUrl;
      count++;
    } else {
      process.stdout.write(`  → No results\n`);
    }
    // Unsplash free tier: 50 req/hour → 200ms間隔で安全
    await new Promise((r) => setTimeout(r, 200));
  } catch (e) {
    process.stdout.write(`  ERROR: ${e.message}\n`);
  }
}

if (!dryRun) {
  writeFileSync(join(ROOT, "data/articles.json"), JSON.stringify(articles, null, 2));
  process.stdout.write(`\nDone: ${count}件 eyecatch追加\n`);
} else {
  process.stdout.write(`\nDry run完了: ${count}件 対象\n`);
}
```

- [ ] **Step 2: dry-runで動作確認**

```bash
node scripts/fetch-unsplash-eyecatch.mjs --dry-run 2>&1 | head -30
```

Expected: 18件の記事スラッグとUnsplashクエリが表示される（URLも取得確認）

- [ ] **Step 3: 本番実行（全18件）**

```bash
node scripts/fetch-unsplash-eyecatch.mjs
```

Expected: `Done: 18件 eyecatch追加`
※ レート制限エラーが出た場合は30秒待って再実行（`--force`なしなら取得済みはスキップ）

- [ ] **Step 4: articles.jsonにeyecatch追加されたか確認**

```bash
node -e "
const a = JSON.parse(require('fs').readFileSync('data/articles.json','utf-8'));
const guides = a.filter(x => !/ranking|budget|-vs-|-showdown-|-comparison-|-alternatives/.test(x.slug));
const withEye = guides.filter(x => x.eyecatch);
console.log('guide記事:', guides.length, '件, eyecatch設定:', withEye.length, '件');
console.log('例:', withEye[0]?.slug, withEye[0]?.eyecatch?.substring(0,60));
"
```

Expected: `guide記事: 18 件, eyecatch設定: 18 件`

- [ ] **Step 5: Supabaseにarticles syncを実行**

```bash
uv run python3 << 'PYEOF'
import json, urllib.request

SUPABASE_URL = "https://pjyoqvwiixzpxbvchvrf.supabase.co"
KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqeW9xdndpaXh6cHhidmNodnJmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjE1NzA4MCwiZXhwIjoyMDkxNzMzMDgwfQ.TkB_IVb0i8tX_rZPSwq7X9uZ1MKTE6ahxQrVmCqkM0Q"

with open("data/articles.json") as f:
    articles = json.load(f)

ok, err = 0, 0
for a in articles:
    if not a.get("eyecatch"):
        continue
    payload = json.dumps({"eyecatch": a["eyecatch"]}).encode()
    req = urllib.request.Request(
        f"{SUPABASE_URL}/rest/v1/articles?id=eq.{a['id']}",
        data=payload, method="PATCH",
        headers={"Authorization": f"Bearer {KEY}", "apikey": KEY,
                 "Content-Type": "application/json", "Prefer": "return=minimal"}
    )
    try:
        with urllib.request.urlopen(req, timeout=10) as r:
            ok += 1
    except Exception as e:
        print(f"ERR {a['slug']}: {e}"); err += 1

print(f"Done: {ok} OK, {err} errors")
PYEOF
```

- [ ] **Step 6: devサーバーでguide記事のヒーロー画像を確認**

```bash
npm run dev
```

http://localhost:3000/articles/family-camp-first-time-guide でUnsplash画像が表示されること

- [ ] **Step 7: コミット**

```bash
git add scripts/fetch-unsplash-eyecatch.mjs data/articles.json
git commit -m "feat: guide記事18件にUnsplash eyecatch追加 + fetchスクリプト"
```

---

## Task 9: git push & 本番デプロイ確認

- [ ] **Step 1: git push**

```bash
git push origin main
```

- [ ] **Step 2: Vercelデプロイ確認**

https://vercel.com のダッシュボードでビルドが成功するまで待つ（約2分）

- [ ] **Step 3: 本番サイトで3記事を確認**

- タイル: https://camp-gear-lab.com/articles/gas-lantern-ranking
- スプリット: https://camp-gear-lab.com/articles/landlock-vs-landnest-shelter
- フォト: https://camp-gear-lab.com/articles/family-camp-first-time-guide

---

## Task 10 (D): imageUrl未設定製品の洗い出しと補完

**Files:**
- Modify: `data/products.json`

- [ ] **Step 1: imageUrl未設定製品を洗い出す**

```bash
node -e "
const p = JSON.parse(require('fs').readFileSync('data/products.json','utf-8'));
const missing = p.filter(x => !x.imageUrl);
console.log('imageUrl未設定:', missing.length, '件');
missing.forEach(x => console.log(' -', x.id, x.name));
"
```

- [ ] **Step 2: 各製品のimageUrlを調査して追加**

楽天市場の商品画像URL（`shop.r10s.jp`）またはメーカー公式を使用。
URLはcurlで200 OKを確認してから設定:

```bash
curl -s -o /dev/null -w "%{http_code}\n" "https://shop.r10s.jp/..."
```

products.jsonの対象製品に `imageUrl` を直接設定する。

- [ ] **Step 3: Supabaseに製品imageUrl更新**

```bash
# 設定したproduct idに対してcurlでPATCH
curl -X PATCH \
  "https://pjyoqvwiixzpxbvchvrf.supabase.co/rest/v1/products?id=eq.{ID}" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqeW9xdndpaXh6cHhidmNodnJmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjE1NzA4MCwiZXhwIjoyMDkxNzMzMDgwfQ.TkB_IVb0i8tX_rZPSwq7X9uZ1MKTE6ahxQrVmCqkM0Q" \
  -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBqeW9xdndpaXh6cHhidmNodnJmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3NjE1NzA4MCwiZXhwIjoyMDkxNzMzMDgwfQ.TkB_IVb0i8tX_rZPSwq7X9uZ1MKTE6ahxQrVmCqkM0Q" \
  -H "Content-Type: application/json" \
  -H "Prefer: return=minimal" \
  -d '{"image_url": "https://shop.r10s.jp/..."}'
```

- [ ] **Step 4: コミット**

```bash
git add data/products.json
git commit -m "fix: imageUrl未設定製品を補完"
git push origin main
```
